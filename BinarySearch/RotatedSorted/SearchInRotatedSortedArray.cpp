#include<bits/stdc++.h>
using namespace std;

#define int long long
#define endl '\n'

class Solution {
    public:
        int findTarget(vector<int>& a,int target) {

        }
};
signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    int target;
    cin >> target;
    vector<int> a(n);
    for(auto& x : a) cin >> x;
    
    return 0;
}